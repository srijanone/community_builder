langcode: en
status: true
dependencies:
  config:
    - flag.flag.report
  module:
    - flag
id: flag_action.report_unflag
label: 'Report Posts'
type: node
plugin: 'flag_action:report_unflag'
configuration:
  flag_id: report
  flag_action: unflag
